<% provide(:title, "購入者一覧" ) %>

<h1>購入者一覧</h1>

<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <table class="table">
      <% @users.each do |user| %>
        <tr>
          <td><%= user.name %></td>
        </tr>
      <% end %>
    </table>
  </div>
</div>